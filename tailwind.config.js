/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./index.html", // Include this if your project uses an HTML entry point.
    "./src/**/*.{js,ts,jsx,tsx}", // Adjust the file extensions as needed.
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
